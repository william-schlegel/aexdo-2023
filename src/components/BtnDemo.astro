---
const label = "Demander une démonstration";
interface Props {
  variant: "menu" | "cta";
}

const { variant } = Astro.props;
---

<a
  href="/contact"
  class={`bg-base-100 text-gray-900 grid place-items-center text-center cursor-pointer font-semibold px-4 py-2 ${
    variant === "menu" ? "" : "w-48 mt-auto text-base"
  } hover:bg-base-100/80`}
>
  {label}
</a>
